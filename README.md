# Assume to have Python3 preinstalled in your device.
- Create a virtual Env.
    - virtualenv venv_name

- Activate the virtual env.
    - source venv_name/bin/activate

- Running the project
    - python manage.py runserver